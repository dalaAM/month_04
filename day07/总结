缓存：把复杂计算或者复杂查询的结果储存起来,提高响应速度;
缓存选择的内容一般选择的数据变化不太频繁
缓存要保证数据的一致性,如果数据发生变化或者有新数据进来，就要将数据重新写入数据
缓存也有时间限制
执行逻辑：
 判断页面或者数据是否有缓存（是否有缓存,缓存或者过期）
  有缓存 返回缓存的页面或数据
  没有缓存 将页面或数据写入缓存,然后将页面或者数据返回

缓存分为：
    整页缓存
    部分缓存

缓存用在Django：
    模板中 （不常用）
    路由中 （不常用）
    view中 （常用）主要缓存的是部分数据
